diff --git a/app/routers/reports.py b/app/routers/reports.py
index f651bd3..6da779b 100644
--- a/app/routers/reports.py
+++ b/app/routers/reports.py
@@ -6,7 +6,7 @@ from datetime import datetime, timedelta
 from io import BytesIO
 from app.database import get_db
 from app.models import IDCardReport, User, ReportStatus, Exam
-from app.schemas import ReportCreate, ReportOut
+from app.schemas import ReportCreate, ReportOut, ReportUpdate
 from app.dependencies import get_current_user, require_role
 
 router = APIRouter(prefix="/reports", tags=["reports"])
@@ -84,6 +84,36 @@ def resolve_report(
     db.refresh(report)
     return report
 
+@router.patch("/{report_id}", response_model=ReportOut)
+def update_report(
+    report_id: int,
+    payload: ReportUpdate,
+    db: Session = Depends(get_db),
+    current_user: User = Depends(get_current_user),
+):
+    """Lets the student who filed the report correct a mistake (e.g. wrong
+    reg no) as long as it hasn't been resolved yet. Editing never changes the
+    tier — tier stays as computed at the original submission time, since it
+    reflects how much notice gate/invigilator staff actually had."""
+    report = db.query(IDCardReport).filter(IDCardReport.id == report_id).first()
+    if report is None:
+        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Report not found")
+    if report.reporter_id != current_user.id and current_user.role.value != "admin":
+        raise HTTPException(
+            status_code=status.HTTP_403_FORBIDDEN,
+            detail="You do not have permission to edit this report",
+        )
+    if report.status == ReportStatus.resolved:
+        raise HTTPException(
+            status_code=status.HTTP_400_BAD_REQUEST,
+            detail="This report is already resolved and can no longer be edited",
+        )
+    for field, value in payload.model_dump(exclude_unset=True).items():
+        setattr(report, field, value)
+    db.commit()
+    db.refresh(report)
+    return report
+
 @router.get("/history/{registration_number:path}", response_model=list[ReportOut])
 def report_history(
     registration_number: str,
diff --git a/app/routers/users.py b/app/routers/users.py
index ddd1017..f791b02 100644
--- a/app/routers/users.py
+++ b/app/routers/users.py
@@ -5,8 +5,10 @@ from sqlalchemy.exc import IntegrityError
 from io import BytesIO
 from app.database import get_db
 from app.models import User
-from app.schemas import UserOut, RoleUpdate
+from app.schemas import UserOut, RoleUpdate, AdminUserUpdate
 from app.dependencies import require_role
+from app.models import UserRole
+from app.schemas import validate_student_regno
 
 router = APIRouter(prefix="/users", tags=["users"])
 
@@ -35,6 +37,44 @@ def update_user_role(
     return user
 
 
+@router.patch("/{user_id}", response_model=UserOut)
+def update_user_profile(
+    user_id: int,
+    payload: AdminUserUpdate,
+    db: Session = Depends(get_db),
+    current_user: User = Depends(require_role("admin")),
+):
+    """Full profile edit for admins — name/email/reg-no, not just role.
+    Students still must keep the 14-digit MUST format; staff stay free-text."""
+    user = db.query(User).filter(User.id == user_id).first()
+    if not user:
+        raise HTTPException(status_code=404, detail="User not found")
+
+    data = payload.model_dump(exclude_unset=True)
+    if "registration_number" in data and user.role == UserRole.student:
+        try:
+            validate_student_regno(data["registration_number"])
+        except ValueError as e:
+            raise HTTPException(status_code=400, detail=str(e))
+
+    if "email" in data:
+        clash = db.query(User).filter(User.email == data["email"], User.id != user_id).first()
+        if clash:
+            raise HTTPException(status_code=400, detail="Email already in use by another user")
+    if "registration_number" in data:
+        clash = db.query(User).filter(
+            User.registration_number == data["registration_number"], User.id != user_id
+        ).first()
+        if clash:
+            raise HTTPException(status_code=400, detail="Registration number already in use by another user")
+
+    for field, value in data.items():
+        setattr(user, field, value)
+    db.commit()
+    db.refresh(user)
+    return user
+
+
 @router.delete("/{user_id}", status_code=204)
 def delete_user(
     user_id: int,
diff --git a/app/schemas.py b/app/schemas.py
index 00e45a6..5c6ee64 100644
--- a/app/schemas.py
+++ b/app/schemas.py
@@ -1,7 +1,23 @@
-from pydantic import BaseModel, EmailStr
+import re
+from pydantic import BaseModel, EmailStr, field_validator, model_validator
 from datetime import datetime, date, time
 from app.models import UserRole, ReportStatus, ReportType
 
+# Students use MUST's official numeric format, e.g. 25101133370022 —
+# starts with the 2-digit intake year (24 or 25) followed by 12 digits.
+# Staff/admin roles are not MUST-registry-issued, so they stay free-text.
+STUDENT_REGNO_PATTERN = re.compile(r"^(24|25)\d{12}$")
+
+
+def validate_student_regno(value: str) -> str:
+    if not STUDENT_REGNO_PATTERN.match(value):
+        raise ValueError(
+            "Student registration number must be 14 digits starting with 24 or 25 "
+            "(e.g. 25101133370022)"
+        )
+    return value
+
+
 class UserCreate(BaseModel):
     full_name: str
     registration_number: str
@@ -10,6 +26,12 @@ class UserCreate(BaseModel):
     role: UserRole = UserRole.student
     security_access_code: str | None = None
 
+    @model_validator(mode="after")
+    def check_student_regno(self):
+        if self.role == UserRole.student:
+            validate_student_regno(self.registration_number)
+        return self
+
 class UserOut(BaseModel):
     id: int
     full_name: str
@@ -32,6 +54,12 @@ class ProfileUpdate(BaseModel):
 class RoleUpdate(BaseModel):
     role: UserRole
 
+class AdminUserUpdate(BaseModel):
+    """Full profile edit for admins — separate from the role-only patch."""
+    full_name: str | None = None
+    email: EmailStr | None = None
+    registration_number: str | None = None
+
 class Token(BaseModel):
     access_token: str
     token_type: str = "bearer"
@@ -66,6 +94,13 @@ class ReportCreate(BaseModel):
     location: str | None = None
     description: str | None = None
 
+class ReportUpdate(BaseModel):
+    """Lets the reporter fix a mistake (e.g. wrong reg no) before it's resolved."""
+    card_owner_reg_no: str | None = None
+    report_type: ReportType | None = None
+    location: str | None = None
+    description: str | None = None
+
 class ReportOut(BaseModel):
     id: int
     reporter_id: int
diff --git a/frontend/admin.html b/frontend/admin.html
index 9e3ac0e..149df27 100644
--- a/frontend/admin.html
+++ b/frontend/admin.html
@@ -11,6 +11,8 @@
     .actions-cell select, .actions-cell button { width:auto; padding:6px 8px; margin:0; font-size:13px; }
     .actions-cell button { background: var(--danger); }
     .actions-cell button.save { background: var(--primary-light); }
+    td input { padding: 6px 8px; font-size: 13px; margin: 0; }
+    .actions-cell button.save-profile { background: var(--accent); border-left-color: var(--primary); }
   </style>
 </head>
 <body>
@@ -27,7 +29,7 @@
 
     <div class="card" id="mainCard" style="display:none;">
       <h1>All Users</h1>
-      <p class="subtitle">Change a user's role or remove an account. You cannot delete your own account.</p>
+      <p class="subtitle">Edit a user's name, reg/staff number, email or role, then press Save. You cannot delete your own account. Student registration numbers must be 14 digits starting with 24 or 25.</p>
       <button id="exportBtn" class="btn btn-secondary" style="margin-bottom:12px;">Download Users (Excel)</button>
       <div class="error-msg" id="errorMsg"></div>
       <div class="success-msg" id="successMsg"></div>
@@ -61,17 +63,17 @@
             return;
           }
           tbody.innerHTML = users.map(u => `
-            <tr data-id="${u.id}">
-              <td>${u.full_name}</td>
-              <td>${u.registration_number}</td>
-              <td>${u.email}</td>
+            <tr data-id="${u.id}" data-role="${u.role}">
+              <td><input class="fullNameInput" value="${u.full_name}"></td>
+              <td><input class="regNoInput" value="${u.registration_number}"></td>
+              <td><input class="emailInput" value="${u.email}"></td>
               <td>
                 <select class="roleSelect">
                   ${ROLES.map(r => `<option value="${r}" ${r === u.role ? "selected" : ""}>${roleLabel(r)}</option>`).join("")}
                 </select>
               </td>
               <td class="actions-cell">
-                <button class="save">Save</button>
+                <button class="save save-profile">Save</button>
                 ${u.id === currentUserId ? "" : '<button class="delete">Delete</button>'}
               </td>
             </tr>
@@ -93,15 +95,29 @@
 
         if (e.target.classList.contains("save")) {
           const newRole = row.querySelector(".roleSelect").value;
+          const originalRole = row.dataset.role;
+          const profilePayload = {
+            full_name: row.querySelector(".fullNameInput").value,
+            registration_number: row.querySelector(".regNoInput").value,
+            email: row.querySelector(".emailInput").value
+          };
           try {
-            await API.request(`/users/${userId}/role`, {
+            await API.request(`/users/${userId}`, {
               method: "PATCH",
-              body: JSON.stringify({ role: newRole })
+              body: JSON.stringify(profilePayload)
             });
-            successMsg.textContent = "Role updated successfully.";
+            if (newRole !== originalRole) {
+              await API.request(`/users/${userId}/role`, {
+                method: "PATCH",
+                body: JSON.stringify({ role: newRole })
+              });
+            }
+            successMsg.textContent = "User updated successfully.";
             successMsg.style.display = "block";
+            row.dataset.role = newRole;
+            loadUsers();
           } catch (err) {
-            errorMsg.textContent = err.data?.detail || "Failed to update role.";
+            errorMsg.textContent = err.data?.detail || "Failed to update user.";
             errorMsg.style.display = "block";
           }
         }
diff --git a/frontend/report.html b/frontend/report.html
index be99948..841639a 100644
--- a/frontend/report.html
+++ b/frontend/report.html
@@ -48,6 +48,17 @@
         <button type="submit">Submit Report</button>
       </form>
     </div>
+
+    <div class="card">
+      <h2>My Reports</h2>
+      <p class="subtitle">Made a mistake? You can edit any report here until it's marked resolved.</p>
+      <div class="error-msg" id="myErrorMsg"></div>
+      <div class="success-msg" id="mySuccessMsg"></div>
+      <table>
+        <thead><tr><th>Reg No</th><th>Type</th><th>Tier</th><th>Status</th><th></th></tr></thead>
+        <tbody id="myReportsBody"><tr><td colspan="5">Loading...</td></tr></tbody>
+      </table>
+    </div>
   </div>
 
   <script src="/static/js/api.js"></script>
@@ -92,14 +103,74 @@
           method: "POST",
           body: JSON.stringify(payload)
         });
-        successMsg.textContent = `Report submitted successfully. Tier: ${report.tier}. Redirecting to dashboard...`;
+        successMsg.textContent = `Report submitted successfully. Tier: ${report.tier}.`;
         successMsg.style.display = "block";
-        setTimeout(() => window.location.href = "/dashboard", 2000);
+        document.getElementById("reportForm").reset();
+        loadMyReports();
       } catch (err) {
         errorMsg.textContent = err.data?.detail || "Failed to submit report.";
         errorMsg.style.display = "block";
       }
     });
+
+    const myUserId = parseInt(decodeJWT(API.getToken()).sub);
+
+    async function loadMyReports() {
+      const tbody = document.getElementById("myReportsBody");
+      tbody.innerHTML = '<tr><td colspan="5">Loading...</td></tr>';
+      try {
+        const reports = await API.request("/reports/");
+        const mine = reports.filter(r => r.reporter_id === myUserId);
+        if (mine.length === 0) {
+          tbody.innerHTML = '<tr><td colspan="5">No open reports.</td></tr>';
+          return;
+        }
+        tbody.innerHTML = mine.map(r => `
+          <tr data-id="${r.id}">
+            <td><input class="regEdit" value="${r.card_owner_reg_no}"></td>
+            <td>
+              <select class="typeEdit">
+                <option value="lost" ${r.report_type === "lost" ? "selected" : ""}>Lost</option>
+                <option value="forgotten" ${r.report_type === "forgotten" ? "selected" : ""}>Forgotten</option>
+              </select>
+            </td>
+            <td><span class="badge badge-tier${r.tier}">Tier ${r.tier}</span></td>
+            <td>${r.status}</td>
+            <td class="actions-cell" style="display:flex; gap:6px;">
+              <button class="saveReport" style="width:auto; padding:6px 10px; margin:0; font-size:13px;">Save</button>
+            </td>
+          </tr>
+        `).join("");
+      } catch (err) {
+        tbody.innerHTML = '<tr><td colspan="5">Failed to load your reports.</td></tr>';
+      }
+    }
+    loadMyReports();
+
+    document.getElementById("myReportsBody").addEventListener("click", async (e) => {
+      if (!e.target.classList.contains("saveReport")) return;
+      const row = e.target.closest("tr");
+      const reportId = row.dataset.id;
+      const myErrorMsg = document.getElementById("myErrorMsg");
+      const mySuccessMsg = document.getElementById("mySuccessMsg");
+      myErrorMsg.style.display = "none";
+      mySuccessMsg.style.display = "none";
+      try {
+        await API.request(`/reports/${reportId}`, {
+          method: "PATCH",
+          body: JSON.stringify({
+            card_owner_reg_no: row.querySelector(".regEdit").value,
+            report_type: row.querySelector(".typeEdit").value
+          })
+        });
+        mySuccessMsg.textContent = "Report updated.";
+        mySuccessMsg.style.display = "block";
+        loadMyReports();
+      } catch (err) {
+        myErrorMsg.textContent = err.data?.detail || "Failed to update report.";
+        myErrorMsg.style.display = "block";
+      }
+    });
   </script>
 </body>
 </html>
diff --git a/frontend/static/css/style.css b/frontend/static/css/style.css
index 453f66b..cfa619a 100644
--- a/frontend/static/css/style.css
+++ b/frontend/static/css/style.css
@@ -1,21 +1,21 @@
 :root {
-  --primary: #14532d;
-  --primary-light: #16a34a;
-  --accent: #d97706;
-  --bg: #f8f9fa;
+  --primary: #1c2f4a;       /* deep academic navy, not the generic AI-purple/indigo */
+  --primary-light: #2d4a73;
+  --accent: #b8860b;        /* muted gold — institutional, not a neon SaaS accent */
+  --bg: #f4f1ea;            /* warm parchment, not flat #fafafa */
   --card-bg: #ffffff;
-  --text: #1a1a1a;
-  --text-muted: #6b7280;
-  --border: #e5e7eb;
-  --radius: 10px;
-  --danger: #dc2626;
-  --success: #16a34a;
+  --text: #20242e;
+  --text-muted: #5b6472;
+  --border: #ddd7c8;
+  --radius: 4px;            /* sharp corners instead of the usual rounded-pill look */
+  --danger: #a3372f;
+  --success: #2f6b3a;
 }
 
 * { box-sizing: border-box; margin: 0; padding: 0; }
 
 body {
-  font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
+  font-family: "Segoe UI", system-ui, -apple-system, sans-serif;
   background: var(--bg);
   color: var(--text);
   min-height: 100vh;
@@ -30,12 +30,19 @@ body {
 .card {
   background: var(--card-bg);
   border: 1px solid var(--border);
+  border-top: 3px solid var(--primary);
   border-radius: var(--radius);
   padding: 24px;
   margin-bottom: 16px;
+  box-shadow: 0 1px 3px rgba(28, 47, 74, 0.06);
 }
 
-h1, h2 { color: var(--primary); margin-bottom: 8px; }
+h1, h2 {
+  font-family: Georgia, "Times New Roman", serif;
+  color: var(--primary);
+  margin-bottom: 8px;
+  letter-spacing: 0.2px;
+}
 p.subtitle { color: var(--text-muted); margin-bottom: 20px; font-size: 14px; }
 
 label {
@@ -66,18 +73,23 @@ button, .btn {
   background: var(--primary);
   color: #fff;
   border: none;
-  border-radius: 8px;
-  font-size: 15px;
+  border-left: 3px solid var(--accent);
+  border-radius: var(--radius);
+  font-size: 14px;
   font-weight: 600;
+  letter-spacing: 0.5px;
+  text-transform: uppercase;
   cursor: pointer;
   text-align: center;
   text-decoration: none;
+  transition: transform 0.08s ease, background 0.15s ease;
 }
 
-button:hover, .btn:hover { background: var(--primary-light); }
+button:hover, .btn:hover { background: var(--primary-light); transform: translateY(-1px); }
+button:active, .btn:active { transform: translateY(0); }
 
-.btn-secondary { background: var(--text-muted); }
-.btn-accent { background: var(--accent); }
+.btn-secondary { background: var(--text-muted); border-left-color: var(--border); }
+.btn-accent { background: var(--accent); border-left-color: var(--primary); }
 
 .error-msg {
   color: var(--danger);
